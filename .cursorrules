# diting-infra 仓库操作规则

本文件为本仓（diting-infra）的 Cursor/AI 操作规则，与文档仓 `diting-doc/00_系统规则_通用项目协议.md` 中 §十一「项目定制化」保持一致。**本文件允许并鼓励提交至本仓库**（见协议 §十一「.cursorrules 与仓库提交」）。

---

## 目录边界 — 配置与部署分离，禁止错放

- **config/**：**仅配置**。仅存放环境与部署**控制用**文件：如 `*.tfvars`、`diting-prod.yaml` / `diting-dev.yaml`（部署控制 YAML）、`deploy.yaml` 等。**禁止**在 config/ 下放置 K8s 部署用 manifest（如 Secret、Job、Deployment 的 YAML）、与某 Chart 绑定的附属资源文件；此类文件归属 **charts/** 下对应 Chart。
- **charts/**：**Chart 及附属 manifest**。每个 Chart（如 `charts/diting-stack/`）包含：`Chart.yaml`、`values.yaml`、`templates/`（Helm 模板）；与该 Chart 配套、需单独 apply 的 K8s YAML（如 ACR 拉取凭证 Secret）放在该 Chart 的 **manifests/** 子目录（如 `charts/diting-stack/manifests/`）。**禁止**将部署用 manifest 放在 config/ 或仓库根下。
- **scripts/**：脚本（如 prod-write-conn.sh）；不存放 K8s 资源定义。
- **deploy-engine/**：子模块，仅读/更新引用，禁止在本仓内修改其内容（见下节）。
- **docs/**：文档说明，不存放可执行部署或配置真相源。

**违规示例**：在 config/ 下创建 `config/manifests/acr-pull-secret.yaml` 等部署文件属**违规**；正确位置为 `charts/diting-stack/manifests/acr-pull-secret.yaml`。AI 与人类在本仓新增 K8s manifest 或“部署文件”时，须放在对应 Chart 下（或 deploy-engine 约定路径），不得放在 config/。

---

## deploy-engine 子模块 — 禁止在子模块拷贝内做任何“写”操作

- **禁止**：在 **diting-infra 仓库内的 `deploy-engine/` 目录下进行任何会改变其 Git 状态或工作树的操作**，包括但不限于：编辑/添加/修改文件，`git add/commit/push`，**`git stash`**。deploy-engine 在本仓内为 **Git 子模块**，真相源为 **deploy-engine 独立仓库**；在子模块拷贝内 stash/commit 会污染该拷贝、影响后续 `make update-deploy-engine` 或 pull main，视为**违规**（见 diting-doc 00_系统规则 §九）。
- **唯一允许的更新方式**：
  1. 在 **deploy-engine 独立仓库**（与 diting-infra 平级的 `deploy-engine/` 根目录）中修改、stash/commit、推送到远程；
  2. 在 **diting-infra** 中**仅**通过子模块同步拉取：`make update-deploy-engine` 或 `git submodule update --init --remote deploy-engine`。
- **正确做法（需暂存或提交修改时）**：到 **deploy-engine 独立仓库** 根目录执行 `git stash` 或 `git commit`，再在 diting-infra 执行 `make update-deploy-engine`；**不得**在 `diting-infra/deploy-engine/` 内执行 stash/commit。
- **AI 执行前自检**：凡待改路径在 `diting-infra/deploy-engine/` 下或需在该目录内执行 git 写命令（含 stash）时，**不得在本仓子模块内执行**，须改为在 deploy-engine 独立仓库内操作，再在本仓执行 `make update-deploy-engine`。
- **AI 行为**：若用户请求在 diting-infra 内修改或操作（含 stash）`deploy-engine/` 下内容，须**拒绝在子模块内执行**，并提示：请到 deploy-engine 独立仓库中修改/stash/commit → 推送远程 → 在 diting-infra 根目录执行 `make update-deploy-engine` 同步更改。

---

## charts/dependencies 目录 — 禁止直接修改 Chart

- **禁止**：在 **`charts/dependencies/`** 目录下**直接更改、添加或修改**任一 Chart 的内容（包括但不限于 `templates/`、`Chart.yaml`、`values.yaml` 等 Chart 内文件）。Chart 须从官方/上游下载到本地后**原样存放**使用。
- **允许**：仅通过 **values 覆盖** 定制行为（如在本仓维护 `values-*.yaml`，或在 `helm install/upgrade` 时使用 `-f`、`--set`）；不得修改 `charts/dependencies/<chart-name>/` 下的 Chart 源码。
- **版本与更新**：Chart 版本在 `Chart.yaml` 或 `Chart.lock` 中显式固定；升级时通过重新下载同路径覆盖或按项目约定流程执行，仍不得在 dependencies 下手改 Chart 内容。

AI 在本仓内执行时须遵守上述限制；若用户请求修改 `charts/dependencies/` 下 Chart 文件，应提示该限制并建议改用 values 覆盖或更新流程。

---

## Makefile/脚本修改 — 部署内容由配置控制

- **原则**：**部署内容**（Service 类型、NodePort、端口、副本数等）由 **config/*.yaml 或 values 覆盖文件** 控制，**禁止在 Makefile 或脚本中写死**；Makefile/脚本仅 `-f config/xxx.yaml` 或从配置读取后传递。
- **禁止**：为**单一组件**（如 Redis、某 DB）在 Makefile 中写专属逻辑：如根据条件生成临时 values、对某组件单独写死 `--set ...=端口/类型`。若某 Chart 的 values 需特殊格式（如某键须为 string），应在 **config/** 下维护该组件的 **values 覆盖文件**（如 `config/redis-values-prod.yaml`），Makefile 仅执行 `helm ... -f config/该文件`。
- **修改前自检**：凡修改本仓 **Makefile 或部署相关脚本**、且涉及某组件的部署/暴露/端口/类型时，须先自检：该决策是否已在 **config/** 或 values 覆盖文件中？若否，**先**在 config 中新增或修改，再改 Makefile/脚本为「仅 -f 或从配置读值传递」。
- **违规示例**：在 Makefile 中为 Redis 写「若 NodePort 则生成临时 YAML」或 `--set master.service.nodePorts.redis=30379` → 违规；正确做法：在 `config/redis-values-prod.yaml` 中写 `master.service.nodePorts.redis: "30379"`，Makefile 仅 `-f config/redis-values-prod.yaml`。

完整约定见 diting-doc `00_系统规则_通用项目协议.md` §十一「部署内容由配置与 Chart 控制」。
