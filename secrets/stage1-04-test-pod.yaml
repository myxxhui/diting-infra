# Stage1-04 L3 验证用测试 Pod，验证后删除
apiVersion: v1
kind: Pod
metadata:
  name: stage1-04-test-pod
  namespace: default
spec:
  restartPolicy: Never
  containers:
  - name: busybox
    image: busybox:1.36
    command: ["sleep", "300"]
    envFrom:
    - secretRef:
        name: diting-akshare-secrets
