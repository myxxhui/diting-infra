# ==============================================================================
# 环境 YAML 示例：default-<env>.yaml（无 project 时）
# ==============================================================================
# 复制到 config/ 并重命名为 default-dev.yaml 或 default-<env>.yaml
# ==============================================================================

global:
  project_name: "diting"
  k3s:
    apiServer:
      domain: "k3s-diting-dev.example.com"
