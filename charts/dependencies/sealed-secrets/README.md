# Sealed-Secrets Chart

放置 **官方最新稳定版** Sealed-Secrets Helm Chart（如 Bitnami sealed-secrets）。

- 从官方仓库拉取：`helm repo add bitnami https://charts.bitnami.com/bitnami && helm pull bitnami/sealed-secrets --untar --untardir .` 或等价命令，将 Chart 内容放入本目录。
- 版本与更新策略见 L3 设计或 DNA 约定。
