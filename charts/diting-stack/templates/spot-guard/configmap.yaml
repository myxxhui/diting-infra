{{- if and .Values.copilot.enabled .Values.spotGuardCron.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: diting-spot-guard-config
  namespace: {{ .Release.Namespace }}
  labels:
    app: diting
    component: spot-guard
data:
  spot-billing-prefs.yaml: |
{{ .Values.spotGuardCron.prefsYaml | nindent 4 }}
{{- end }}
