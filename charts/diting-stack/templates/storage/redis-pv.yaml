{{- if .Values.storage.redis.enabled }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.storage.redis.pv.name }}
  labels:
    app: diting
    component: redis
    type: static-storage
spec:
  capacity:
    storage: {{ .Values.storage.redis.pv.capacity }}
  accessModes:
    - {{ .Values.storage.redis.pvc.accessMode }}
  persistentVolumeReclaimPolicy: {{ .Values.storage.redis.pv.reclaimPolicy }}
  storageClassName: ""
  hostPath:
    path: {{ .Values.storage.dataPath }}/{{ .Values.storage.redis.pv.subPath }}
    type: DirectoryOrCreate
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: Exists
{{- end }}
