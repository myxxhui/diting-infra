{{- if .Values.storage.timescaledb.enabled }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.storage.timescaledb.pv.name }}
  labels:
    app: diting
    component: timescaledb
    type: static-storage
spec:
  capacity:
    storage: {{ .Values.storage.timescaledb.pv.capacity }}
  accessModes:
    - {{ .Values.storage.timescaledb.pvc.accessMode }}
  persistentVolumeReclaimPolicy: {{ .Values.storage.timescaledb.pv.reclaimPolicy }}
  storageClassName: ""
  hostPath:
    path: {{ .Values.storage.dataPath }}/{{ .Values.storage.timescaledb.pv.subPath }}
    type: DirectoryOrCreate
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: Exists
{{- end }}
