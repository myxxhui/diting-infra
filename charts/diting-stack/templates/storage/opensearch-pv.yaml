{{- if .Values.storage.opensearch.enabled }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.storage.opensearch.pv.name }}
  labels:
    app: diting
    component: opensearch
    type: static-storage
spec:
  capacity:
    storage: {{ .Values.storage.opensearch.pv.capacity }}
  accessModes:
    - {{ .Values.storage.opensearch.pvc.accessMode }}
  persistentVolumeReclaimPolicy: {{ .Values.storage.opensearch.pv.reclaimPolicy }}
  storageClassName: ""
  hostPath:
    path: {{ .Values.storage.dataPath }}/{{ .Values.storage.opensearch.pv.subPath }}
    type: DirectoryOrCreate
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: Exists
{{- end }}
