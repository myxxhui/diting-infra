{{- if and .Values.copilot.enabled .Values.storage.copilotReports.enabled }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.storage.copilotReports.pv.name }}
  labels:
    app: diting
    component: copilot-reports
    type: static-storage
spec:
  capacity:
    storage: {{ .Values.storage.copilotReports.pv.capacity }}
  accessModes:
    - {{ .Values.storage.copilotReports.pvc.accessMode }}
  persistentVolumeReclaimPolicy: {{ .Values.storage.copilotReports.pv.reclaimPolicy }}
  storageClassName: ""
  hostPath:
    path: {{ .Values.storage.dataPath }}/{{ .Values.storage.copilotReports.pv.subPath }}
    type: DirectoryOrCreate
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: Exists
{{- end }}
